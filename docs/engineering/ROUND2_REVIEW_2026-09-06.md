# 第二轮审查：从工程候选到受控内测

基准：PR #3，`6b944e6f1abd86b9da9fc0ee1560f96fa8ca19b3`。本轮仍在 `fix/native-integrity-deps-20260906`；不修改 main，不新增平行实现、不发布、不新增依赖。

## 审查与已落实的修改

| 项目 | 基准中的具体问题 | 本轮处理 / 验证入口 |
| --- | --- | --- |
| 请求大小与耗时 | cinema 只信 Content-Length；events 先读完整文本再检查；慢流可占用读取 | 共用 readBoundedJson，按真实 UTF-8 字节计数、超限取消、读取超时；两接口实际调用测试 |
| 限流状态 | 每个地址保留一条记录，标识不断变化可扩大 Map | 有上限的时间窗，满时拒绝新标识，不驱逐尚有效的老桶；仍不是分布式限流 |
| 模型输出 | scene label 漏检，空白视角可能被接受 | 检查所有展示字段，含截断前内容；空关键视角回退；模型响应包限制64KiB |
| URL评分 | activation=999 等非法值可能被夹成1–5自评 | 当前量表仅整数1–5，历史量表仅整数0–10；非法值不标 supplied |
| 周报归因 | 一个方法使用最多，另一个方法反馈好，前者也可能被称为有效 | 只按同一方法已完成且反馈better的记录推荐；标题不声称普遍效果；时长须在该方法目录中 |
| 内测分析 | 空评分变成0；不足15人时先返回而漏掉已发生的不适 | 空值保留缺失；暂停信号从第一人开始生效；明确安全观察缺失时不放行 |
| CSV质量 | 简单按行拆分破坏多行引号；重复ID可凑满人数 | 支持引号换行、逗号及转义；坏格式、缺ID、重复ID阻止扩测 |
| 撤回统计同意 | 仅清空sink不能取消等候本机ID的异步发送 | 同意生命周期附带AbortSignal；关闭时同步取消，旧队列不能因再次同意复活 |
| 交接漂移 | 文档仍说PR3以PR1为base，隐私草稿保留旧假设 | 明确PR3直接对main；上架前置说明覆盖旧草稿，不改已批准事实 |

请求限制是单实例的防御层，不认证调用者；x-forwarded-for仍须由可信代理覆盖。公开带付费模型key的接口，发布前还要验证网关/平台限流、总预算和请求来源。不能用CORS或本地Map声称防滥用完成。词法输出过滤也不能证明任意模型输出都安全。

## 验证方式

新增 `test:round2`：11项API/流/评分/归因/统计撤回契约测试，8项内测分析测试。均为合成数据，未调用真实StepFun或PostHog。

新增 `scripts/smoke-adversarial.mjs`，接入已有 `smoke:web`：非法URL评分；连续点击；HTML样式输入保持文本；已加载后的断网退出；刷新保留旧尝试；320px及减少动效下实际点击停止；production HTTP请求大小限制。原有10组Web与5组Expo Web场景保留。

源码存在不代表检查已通过。完整结果看本轮head对应PR Checks；baseline的100项通过只证明baseline。截图在 `web-smoke/round2`，报告绑定测试提交。作者本地仅运行无依赖目标测试及脚本语法检查；完整npm ci、typecheck、lint、构建与浏览器由CI执行。

## 已审查但未扩大的范围

- dependency overrides 与 `patch-query-string-cjs.mjs` 保留：锁定已知版本/字符串、幂等、未知版本失败，不下载执行额外代码；本轮未改锁文件。
- 原生保存/删除串行队列、过期AI请求与开始资格沿用PR3；旧数据不自动“修正”为真实新量表证据。
- 默认Web练习本地匹配与脚本，不因服务端有key就变成实时AI。原生direct仍离线；可选AI按当前开关/明确操作执行。
- 撤回可取消尚未发送或在途请求，不能撤回服务商已经收到的数据；需在隐私说明中保持此边界。
- 字数、负面字幕、方法命名与“灵修”理解仍是待真人观察的问题，不再通过AI审美投票重做页面。

## 不能由本轮代办的门槛

真实Safari/微信/Android、键盘/语音/辅助功能体验；签名安装；真实供应商延迟/处理政策；权利人授权；支持渠道验收；15位真实用户、复用和付费证据。看 `SEED_TEST_LAUNCH_PACK_ZH.md`、`CONTENT_RIGHTS_MATRIX.md` 和 `PREFLIGHT_2026-09-06.md`。

不宣称“全面渗透测试”“零风险”或临床有效。对抗测试范围是明确列出的本机合成场景。
